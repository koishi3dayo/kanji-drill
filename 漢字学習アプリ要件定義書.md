# 漢字学習アプリ 要件定義書

- **プロジェクト名**：漢字ドリル（小学生向け 漢字学習アプリ）
- **作成日**：2026-07-03
- **バージョン**：v2.8（予習／復習モード・習った漢字リスト：実装完了）
- **開発手法**：AI-DLC（AI-Driven Life Cycle）

---

## 1. 背景・目的

小学2〜5年生（特に4〜5年生重点）の漢字定着を、**毎日10問のドリル**で習慣化する。
学習の継続と達成感を、進捗の可視化（ヒートマップ）とゲーミフィケーション（連続日数・バッジ）で支える。
**（v2.7）対象範囲を小学6年生・中学1年生まで拡張し、出題数（10問／20問）と出題順序（ランダム／五十音順）を選べるようにする。**

---

## 2. 仕様変更の記録

### 2.1 変更の経緯

| 版 | 入力・採点方式 | 課題 |
|----|--------------|------|
| v1.0（当初） | PCのキーボード＋IMEで漢字を入力して自動採点 | IMEが漢字を候補表示するため「思い出して書く」練習にならない。学習効果が薄い |
| v1.5（検討） | タブレット/スマホで手書きキャンバス入力 | 手書き対応端末が必要。本PCは非対応 |
| **v2.0（本仕様・確定）** | **PC出題 → 紙に手書き回答 → PCで答え合わせ・自己採点** | 課題を解消。本物の書き練習は紙で行う |

### 2.2 v2.0 の基本方針（確定仕様）

1. **電子端末への手書き入力は行わない。**
2. **PCが問題を出題する。**（読み・熟語・用例のヒントを画面表示）
3. **児童は問題を見て、紙に漢字を手書きで回答する。**
4. **PCで正解を表示し、児童本人または保護者が答え合わせをする。**
5. **正解／不正解をPC上でチェックボックス（○×）で記録する。**
6. **記録の履歴から、学習進捗と弱点をヒートマップで可視化する。**

---

## 3. 対象ユーザー

| 区分 | 内容 |
|------|------|
| 主ユーザー | 小学2〜5年生（4〜5年生を重点）。**（v2.4）本アプリを利用する3人の子ども（1号・2号・3号）。学年はそれぞれ異なる** |
| 補助ユーザー | 保護者（答え合わせの補助・進捗確認） |
| 教科・範囲 | 国語／学年別配当漢字（2〜5年生、約550字）。**（v2.7）小学6年生・中学1〜3年生を追加し、2年生〜中学3年生までをカバー** |

---

## 3.5 プロフィール管理（v2.4 新規・実装完了）

### 3.5.1 背景
1家庭に子どもが3人（1号・2号・3号）おり、学年がそれぞれ異なる。1台の端末を共用するが、**進捗・実績カレンダー・出題の重み付けは子どもごとに独立して管理する**必要がある。

### 3.5.2 仕様（確定事項）
- **プロフィールは「1号」「2号」「3号」の3人固定。**（4人目以降の追加や動的な人数変更は本仕様では対象外）
- 各プロフィールは以下を個別に持つ：
  - **学年（2〜6年・中学1〜3年から選択）**：出題の重み付けの基準になる（§4.1.1）。**（v2.7）6年生・中学1〜3年を選択肢に追加**
  - **出題数（10問／20問、v2.7新規）**：1回のテストで出す問題数
  - **出題順序（ランダム／五十音順、v2.7新規）**：問題を並べる順序
  - **学習モード（予習／復習、v2.8新規）**：出題の元になる漢字プールの選び方（§4.1.4）
  - **習った漢字リスト（v2.8新規）**：学校で習った漢字の記録。復習モードの出題元になる（§4.1.5）
  - **学習履歴**（実施日・出題内容・正誤）→ 学習カレンダーに反映
  - **漢字ごとの正答統計**（出題回数・正解数）→ しんちょくマップ（ヒートマップ）に反映
  - **連続日数（ストリーク）・バッジ**
- **プロフィール切り替えUI**をホーム画面の最上部に設置する（例：`[1号] [2号] [3号]` の3択タブ）。
  - 選んだプロフィールが「現在アクティブな子ども」となり、以降の操作（学年選択・今日の10問・進捗確認・カレンダー確認）はすべてそのプロフィールに対して行われる。
  - これにより「今日の10問を始める、と同じ画面で誰がやるか選ぶ」「トップ画面で誰が使うか選ぶ」という2つの要望を、**1つのプロフィール切り替えUI**で同時に満たす（同じ選択が両方の目的を兼ねる）。
- **学年選択UI**を、プロフィール切り替えのすぐ下に設置する（例：`[2年] [3年] [4年] [5年] [6年] [中1] [中2] [中3]` タブ、現在の選択がハイライト）。
  - アクティブなプロフィールの学年を表示・変更する。変更すると即座にそのプロフィールの出題重み付け（§4.1.1）に反映される。
  - 学年は年に数回程度しか変わらない想定のため、**「毎回選び直す」のではなく「プロフィールに保存された設定を、必要な時だけ変更する」形**にする。
- **（v2.7）出題数選択UI・出題順序選択UIを、学年選択のすぐ下に追加する。**
  - 出題数：`[10問] [20問]` の2択タブ。
  - 出題順序：`[ランダム] [五十音順]` の2択タブ。
  - いずれもプロフィールに保存され、変更した回から反映される。
- **（v2.8新規）学習モード選択UIを、出題数・出題順序と同じ並びに追加する。**
  - `[予習モード] [復習モード]` の2択タブ。
  - **予習モード**：これまでどおりの動作（§4.1.1の学年重み付け）。**変更なし。**
  - **復習モード**：習った漢字リスト（§4.1.5）から出題する（§4.1.4）。
- **（v2.8新規）「習った漢字を選ぶ」ボタンをホーム画面に追加する。**
  - タップすると、**現在の学年の漢字だけ**を一覧表示し、学校で習った漢字をチェック（複数選択）できる画面に遷移する。
  - 一つ下以下の学年は選択操作が不要（自動的に全て「習った」扱いになるため、§4.1.5）。
  - 初期状態は**全て未選択**。保護者・本人がまとめてチェックを入れていく。
  - 選択件数（例：「132/191字 選択ずみ」）を画面に表示し、選び忘れを防ぐ。
- ホーム画面のストリーク・週間成績・バッジ・「今日の10問を始める」ボタンは、**すべてアクティブなプロフィールのデータを表示する。**（v2.7以降は出題数に応じて「今日の◯問を始める」と表示）
- **しんちょくマップ（ヒートマップ）・学習カレンダーも、アクティブなプロフィールのデータのみを表示する。**（画面上部に「1号の記録」等、現在どのプロフィールを見ているか分かる表示を出す）
- **漢字いちらん画面（漢字辞書としての一覧）は共通データのため、プロフィールに依存せず全員共通のまま**とする（漢字そのものの読み・熟語は学習進捗ではないため）。

### 3.5.3 データ保存構造（設計案）
現行の単一構造から、プロフィールごとに分離した構造へ変更する。

```jsonc
{
  "activeProfile": "1",              // 現在アクティブな子ども ("1"|"2"|"3")
  "profiles": {
    "1": {
      "name": "1号", "grade": "5", "qCount": 10, "orderMode": "random",
      "studyMode": "preview",          // "preview"(予習) | "review"(復習)
      "learnedByGrade": { "5": ["歌","算","給", "..."] },  // 現在の学年ぶんの明示選択（下の学年は自動で全て習った扱い）
      "history": {...}, "stats": {...}, "streak": {...}
    },
    "2": { "name": "2号", "grade": "3", "qCount": 10, "orderMode": "random", "studyMode": "preview", "learnedByGrade": {}, "history": {...}, "stats": {...}, "streak": {...} },
    "3": { "name": "3号", "grade": "2", "qCount": 10, "orderMode": "random", "studyMode": "preview", "learnedByGrade": {}, "history": {...}, "stats": {...}, "streak": {...} }
  }
}
```
- **（v2.7新規）`qCount`**（10 or 20）・**`orderMode`**（"random" or "gojuon"）をプロフィールに追加。未設定時は `qCount:10`・`orderMode:"random"`（従来どおりの挙動）にフォールバックし、既存データへの後方互換を保つ。
- **（v2.8新規）`studyMode`**（"preview" or "review"）・**`learnedByGrade`**（学年コード→習った漢字の配列、のマップ）をプロフィールに追加。未設定時は `studyMode:"preview"`・`learnedByGrade:{}`（＝下の学年のみ習った扱い）にフォールバックし、既存データへの後方互換を保つ。
  - `learnedByGrade` は**現在選択中の学年のキーだけ**を読む（下の学年は常に自動で全て含まれるため保存不要）。学年を変更しても、以前の学年で選んだ内容はキーごと保持され、その学年に戻したときに復元される。
- `history` / `stats` / `streak` の中身は既存仕様（§5.2・§6）と同じ形式を、プロフィール単位で持つ。
- **移行措置**：旧バージョンの単一データが端末に残っている場合、初回起動時にそれを「1号」のデータとして自動移行し、消失させない。

---

## 4. 出題仕様

### 4.1 出題数・頻度
- **1日1回のセッションを基本とする**（再挑戦は可）。
- **10問すべてを最初から画面に一覧表示する。**1問ずつの逐次出題は行わない。
- **（v2.7新規）出題数はプロフィールごとに「10問」「20問」から選択できる。**（§3.5.2、§4.1.2）
- **（v2.7新規）出題順序はプロフィールごとに「ランダム」「五十音順」から選択できる。**（§4.1.3）

### 4.1.1 出題学年の重み付け（v2.4 改訂：プロフィールの学年に応じて可変／v2.7で6年・中学1〜3年に対応）

v2.2では「5年生固定・9:1」だったが、v2.4では**アクティブなプロフィールの学年設定（§3.5）に応じて重み付けが変わる。**v2.7では対象学年に6年生・中学1〜3年を追加し、学年の並び順は
`2年 → 3年 → 4年 → 5年 → 6年 → 中学1年 → 中学2年 → 中学3年` とする（各学年の「一つ下」は配列上の前の学年。例：中学2年の一つ下＝中学1年）。

| プロフィールの学年 | 出題比率（10問設定の場合） |
|---|---|
| 2年生 | 2年生の漢字が **10問中10問**（2年生より下の学年がないため全問2年生） |
| 3年生 | 3年生の漢字が **10問中9問**、残り1問は主に2年生（一つ下の学年） |
| 4年生 | 4年生の漢字が **10問中9問**、残り1問は主に3年生（一つ下の学年） |
| 5年生 | 5年生の漢字が **10問中9問**、残り1問は主に4年生（一つ下の学年） |
| **6年生（新規）** | 6年生の漢字が **10問中9問**、残り1問は主に5年生（一つ下の学年） |
| **中学1年（新規）** | 中学1年の漢字が **10問中9問**、残り1問は主に6年生（一つ下の学年） |
| **中学2年（新規）** | 中学2年の漢字が **10問中9問**、残り1問は主に中学1年（一つ下の学年） |
| **中学3年（新規）** | 中学3年の漢字が **10問中9問**、残り1問は主に中学2年（一つ下の学年） |

- 「主に」としているのは、**前日の不正解の復習を優先する際、その復習問題が必ずしも一つ下の学年とは限らないため**（例：5年生プロフィールで3年生の問題を前日間違えていた場合、その復習を1問枠に充てる）。復習対象がない場合は、一つ下の学年から正解率の低い漢字・未出題の漢字を優先して選ぶ。
- 前日不正解の復習を優先する際も、上記の学年比率（例：5年生プロフィールなら9:1）は維持する。
- **20問設定の場合は、上記の比率を20問ベースに換算する**（例：5年生なら18問＋一つ下の学年2問。§4.1.2）。

### 4.1.2 出題数の選択（v2.7 新規・実装完了）
- プロフィールごとに **10問／20問** を選択できる（§3.5.2）。
- 学年重み付け（§4.1.1）の比率はそのまま維持し、問題数に応じて比例配分する。
  - 10問：主学年9問／一つ下1問（2年生のみ全問主学年）
  - 20問：主学年18問／一つ下2問（2年生のみ全問主学年）
- 答え合わせ・結果画面・見返し画面・カレンダーはすべて出題数に連動して表示件数が変わる（画面固定表示の「10問」表記は撤廃し、実際の問題数を表示する）。
- バッジの「がんばった回数」「週の習慣」等は**実施した回数（日数）で判定**しており、1回あたりの問題数（10問/20問）による影響は受けない（現状のまま）。

### 4.1.3 出題順序の選択（v2.7 新規・実装完了）
- プロフィールごとに **ランダム／五十音順** を選択できる（§3.5.2）。
- **ランダム**：これまでどおり、選ばれた問題をシャッフルして表示する（既定値・現行動作を維持）。
- **五十音順**：選ばれた問題を、教育漢字配当表の並び順（＝ `kanji-data.js` に収録されている順序。実質的にあいうえお順に近い並び）に沿って表示する。
  - 前日の不正解の復習や学年の重み付け（§4.1.1）で「どの漢字が選ばれるか」は変わらない。**変わるのは表示する並び順のみ。**

### 4.1.4 学習モード：予習／復習（v2.8 新規・実装完了）

**背景・課題**：これまでの出題（§4.1.1）は「プロフィールの学年を中心に、まだ習っていないかもしれない漢字も含めて」出題する、いわば**予習型**の出題ロジックだった。これに加え、**学校で実際に習った範囲だけに絞って復習したい**という要望に対応する。

**仕様**
- プロフィールごとに **予習モード／復習モード** を選択する（§3.5.2）。
- **予習モード**：§4.1.1の学年重み付けロジックをそのまま使用する。**動作は一切変更しない。**
- **復習モード**：出題プールを「習った漢字リスト」（§4.1.5）に限定する。学年重み付け（9:1等）は使わず、**習った漢字リストの中から**、以下の優先順で選ぶ：
  1. **前日に間違えた漢字のうち、習った漢字リストに含まれるもの**（＝「習った漢字」かつ「前回間違えた」の両方を満たす問題）を優先的に出題する。
  2. 残りの枠は、習った漢字リストの中から**正解率の低い漢字・未出題の漢字を優先**して選ぶ（＝新たな漢字の出題）。
  3. 1・2をあわせて出題数（§4.1.2：10問または20問）に達するまで選ぶ。
- 出題数（10問/20問）・出題順序（ランダム/五十音順）の設定は、予習・復習どちらのモードでも共通して適用される。
- 学年の重み付け（9:1）は**予習モードのみの概念**であり、復習モードには存在しない（習った漢字リストという単一のプールから選ぶため）。

### 4.1.5 習った漢字リスト（v2.8 新規・実装完了）

**仕様**
- 「習った漢字リスト」＝ **(A) プロフィールの学年より下の学年の漢字（自動・全て）** ＋ **(B) 現在の学年のうち、ユーザーが明示的に「習った」とチェックした漢字**。
  - 例：プロフィールが5年生の場合 → 2・3・4年の漢字は自動的に全て「習った」扱い。5年の漢字は、ホーム画面の「習った漢字を選ぶ」（§3.5.2）でチェックした分だけが対象になる。
- チェックは学年ごとに保持され、学年を変更してもチェック内容は消えない（§3.5.3）。
- 復習モードで出題プールが空（=現在の学年で1つも選択しておらず、かつ一番下の学年〈2年生〉の場合など）になった場合は、ホーム画面に「まずは『習った漢字を選ぶ』でチェックしてね」という案内を表示し、テスト開始をブロックする。

### 4.1.6 熟語問題の全文字習得判定と代替問題（v2.8.1 バグ修正・実装完了）

**背景（発見された不具合）**：v2.8の復習モードは「出題が登録されている行の代表漢字1文字」だけで習得判定していた。しかし熟語問題は答えが複数文字（例：「複雑」を書かせる問題は「複」の行にあるが答えは複＋雑）。そのため代表漢字（複）が習得済みなら、**答えに含まれる未習の相方漢字（雑）ごと出題**されてしまい、§4.1.5の意図に反していた。該当は熟語問題438問中111問。

**修正仕様**
- 復習モードでは、**答え（`{{ }}`内）の全漢字が習得済み**の場合のみその問題をそのまま出題する。
- 未習の相方漢字を含む場合は、**その漢字単独で成立する代替問題に差し替える**（`sentence-data-alt.js`、71字：訓読み41字＋下位学年の相方を持つ熟語30字）。
  - 例：訓読みあり → 園→「はなの園」／音読みのみ → 複→「複数」（複雑ではなく）。
- 代替も無く未習相方を含む38字（例：科・京・雑・演）は、相方も習得済みになるまで復習出題しない（既知の制約。無理な代替を作らない方針）。
- 代替問題の周囲は誤変換防止のため既習漢字化せず、対象以外はかな表記。
- **予習モード（§4.1.1）は影響を受けない**（従来どおり）。

### 4.2 問題タイプ（v2.1 改訂）
| タイプ | 出題内容（画面表示） | 紙の回答 |
|--------|---------------------|---------|
| 書き（訓読み中心） | かなの短文を提示（下線部が対象） | 下線部を漢字**（送りがなを含む）**で書く（§4.2.2） |
| 熟語（音読み中心） | かなの短文を提示（下線部が対象） | 下線部の熟語を漢字で書く |

- 出題はすべて**5〜10文字程度のかな短文**とする。単語だけの出題はしない。
  - NG：「その」→ 園
  - OK：「うたをうたう」→ 歌を歌う ／「こうえんであそぶ」→ 公園であそぶ
- 短文データは `sentence-data.js` に全漢字ぶん収録（かな文・漢字文・タイプ）。
- 音読み・訓読み・熟語をバランスよく混在させる。
- **（v2.2）問題文中、出題対象（答えさせる箇所）以外の部分は、極力すでに習った漢字に変換して表示する。**
  - 例：「となりのへやにいどう」（全部かな）→「となりの部屋に移動」（対象の「いどう」以外はすでに習った熟語「部屋」で表示）
  - 全文かな表記は避け、実際の文章に近い読みごたえのある問題にする。
  - データ形式を `{tpl, reading, type}` に刷新（`tpl` は `{{対象}}` マーカーで出題箇所を示す既習漢字まじりテンプレート）。
  - **実装方法**：`kanji-data.js` の熟語（2文字以上の単語＋読み）を辞書化し、既存の短文データへ機械的に置換するスクリプト（`scripts/convert_sentences.py`）で全550件超を一括変換。
    - 単漢字の訓読み（1文字の読み）は助詞（の・に・を等）と同音衝突し誤変換を起こすため、**辞書化の対象外**とした（例：「の」が「野」に誤変換される事故を確認し除外）。
    - 変換後、辞書内の熟語どうしの同音異義語衝突（例：「こうじょう」＝向上／工場）を全件突合検証し、誤った語が採用されていないことを確認済み。

### 4.2.1 用語の注記
- 「用例」タイプは v2.2 にて「書き」「熟語」の2タイプに整理・統合済み（4.2参照）。

### 4.2.2 送りがなを含めた下線・正解（v2.9 仕様変更・実装完了）

**目的**：送りがなのある訓読みで、送りがなも含めて学べるようにする。下線を「漢字になる部分」だけでなく**送りがなも含めた範囲**に引き、正解も送りがな込みで書かせる。

- **対象**：送りがなを持つ**訓読み**の「書き」タイプのみ。
- **対象外**：
  - 送りがな無しの訓読み（例：顔＝かお、雲＝くも、羽＝はね）→ 従来どおり漢字部分のみ。
  - 「熟語」タイプ（音読み中心、送りがなの概念がない）→ 従来どおり。
- **変更例**

  | 漢字 | 現状（下線＝漢字部分のみ） | 変更後（下線＝送りがな込み） | 正解 |
  |------|--------------------------|----------------------------|------|
  | 強 | <u>つよ</u>いかぜ | <u>つよい</u>かぜ | 強い |
  | 楽 | <u>たの</u>しい | <u>たのしい</u> | 楽しい |

### 4.2.3 接続助詞をまたぐ複数下線（v2.9 仕様変更・実装完了）

**目的**：1つの短文に出題対象が2つあり接続助詞（を・と等）でつながる場合、対象ごとに独立した下線を引き、助詞は下線対象外（地の文）にする。

- **変更例**

  | 漢字 | 現状（1本の下線） | 変更後（2本の下線・助詞は地の文） | 正解 |
  |------|------------------|-----------------------------------|------|
  | 歌 | <u>うたをうたう</u> | <u>うた</u>を<u>うたう</u> | 歌を歌う |
  | 数 | <u>かずをかぞえる</u> | <u>かず</u>を<u>かぞえる</u> | 数を数える |
  | 親 | <u>おやとこ</u>ども | <u>おや</u>と<u>こ</u>ども | 親と子ども |
  | 組 | あか<u>ぐみとしろぐみ</u> | あか<u>ぐみ</u>と<u>しろぐみ</u> | あか組としろ組 |
  | 伝 | <u>でんごんをつたえる</u> | <u>でんごん</u>を<u>つたえる</u> | 伝言を伝える |
  | 祖 | <u>そふとそぼ</u> | <u>そふ</u>と<u>そぼ</u> | 祖父と祖母 |

- 該当は現データ中**6件**（上記全て）。
- **§4.2.2 との複合**：「伝言を伝える」は§4.2.3（助詞で分割）と§4.2.2（送りがな込み）が同時に適用され、下線は「でんごん」「つたえる」の2本、「を」は地の文、送りがな「える」は下線内・正解は「伝言を伝える」。

### 4.2.4 §4.2.2・4.2.3 の実装内容（v2.9）
- **データ形式拡張**：`tpl`は`{{ }}`を複数含められるようにし（`TPL_RE`をグローバル化）、`reading`は従来の文字列（`{{}}`が1つ）に加え、出現順に対応する読みの**配列**（`{{}}`が複数）にも対応。既存データは無改修で動作（後方互換）。
- **接続助詞分割（§4.2.3）**：該当6件（歌・親・数・組・伝・祖）を手動で分割データに書き換え。
- **送りがな込み（§4.2.2）**：`scripts/merge_okurigana.py`で自動検出（`kanji-data.js`の訓読みハイフン表記を手がかりに、`{{}}`直後の文字列が送りがなと一致するものを`{{}}`内へ統合）。**229件を自動変換して反映済み**。
- **既知の制約（残タスク・随時対応）**：
  - 自動検出で一致しなかった「要確認」候補が約170件あるが、その大半は元々送りがな不要な訓読み（顔＝かお 等）で対象外が正しい。ただし「近く（ちかく）」のように送りがな候補の語形違いで一部見落としがある可能性があり、気づいたら随時修正する。
  - 今回の調査中に、本仕様変更とは無関係の**既存データ不具合**（`sentence-data.js`内に「未知」「仮名」「意味」等の不自然な単語が混入している箇所が十数件）を発見。過去の既習漢字自動変換処理での同音誤変換とみられる。**別issueとして追って対応**（v2.9の対象外）。
    - （v2.9.1で発見・修正済み）同種の誤変換を新たに発見：`"在":[{tpl:"{{現在}}の事故く",reading:"げんざい",type:"熟語"}]` → 「げんざいの時刻」に修正（"時刻"が"事故"+"く"に誤変換された跡）。周辺に「事故」を含む他3箇所も点検し、いずれも正しい文（事故の原因／事故をふせぐ／事故の死亡者）であることを確認済み。
  - **`sentence-data-alt.js`（復習モードの代替問題データ、71件）は§4.2.2（送りがな込み）の変換が未適用だった**（v2.9.1で対応済み。下記参照）。

### 4.2.5 v2.9.1 修正内容（実装完了）
利用者が復習モードで確認した表示不具合3件に対応：

1. **「在」のデータ誤り**：`げんざいの事故く` → `げんざいの時刻` に修正。
2. **「数を数える」の表示が分割されていない件**：`sentence-data.js`側は既にv2.9で修正済みだった（`"かず"を"かぞえる"`）。復習モードでの見え方の原因切り分けは完了せず（ブラウザキャッシュの可能性を含む）。ただし3の対応と合わせて代替問題データも修正済みのため、再発なしを期待。
3. **送りがなに下線が引かれていない件**：`sentence-data-alt.js`にv2.9の送りがな統合（§4.2.2）が未適用だったのが原因。`scripts/merge_okurigana_alt.py`を新規作成し、`kanji-data.js`の訓読みハイフン表記を手がかりに自動検出、**25件を自動変換して反映済み**。ブラウザ上で`Quiz.getReviewCandidate()`経由の実際の復習フローでも送りがな込み表示を確認済み（例：告→「けっかを<u>つげる</u>」）。

### 4.2.6 v2.9.2 データ修正（実装完了）
利用者から報告された不具合2件に対応：

1. **「パンをかう」の下線範囲**：データを確認したところ`"買":[{tpl:"パンを{{買う}}",reading:"かう",type:"書き"}]`となっており、**§4.2.2の送りがな込み変換は既に適用済み**だった（v2.9で229件自動変換した中に含まれる）。表示側の原因は、§4.2.5と同じ**ブラウザ/Service Workerの古いキャッシュ**（同一端末で前回も発生・キャッシュクリアで解消した実績あり）。→ **コード側の修正は不要**と確認。
2. **「洗濯ものを干す」が「選択ものを干す」と表示される件（データ誤り・修正済み）**：`"干":[{tpl:"選択ものを{{干す}}",reading:"ほす",type:"書き"}]`が原因。既習漢字の自動かな→漢字変換時に、同音異義語「せんたく」が「洗濯」ではなく辞書内の「選択」（`選`の熟語）に誤って変換されたもの（§4.2.4記載の誤変換バグと同一クラス）。
   - **重要な制約**：「濯」は本アプリの収録漢字（`kanji-data.js`）に存在しない（小学校配当外／未収録）ため、「洗濯」を漢字表記することはできない。
   - **修正内容**：`選択ものを{{干す}}` → `せんたくものを{{干す}}`に修正（対象外の部分をかな表記に戻す）。出題対象（`干`・`ほす`）自体への影響なし。

### 4.2.7 v2.10.1 誤変換の全件調査・修正（実装完了）

**背景**：「選択→洗濯」誤変換の発覚を受け、同種の誤変換が他にも無いか、全学年・予習/復習両モードのデータ（`sentence-data.js`／`sentence-data-alt.js`）を対象に調査した。

**バグの正体**：既習漢字の自動かな→漢字変換（§4.2 v2.2）が、単語の境界を無視して「読みが一致する熟語」を機械的に置換するため、以下の2パターンで誤変換が発生していた。
1. **単漢字の訓読みと熟語の衝突**：「みち」＝単漢字`道`の訓読み（辞書化対象外）と、熟語`未知`（辞書登録あり）が同じ読みで衝突し、`道`が使われるべき箇所に`未知`が採用された。同様に「かぜ」＝単漢字`風`と熟語`風邪`が衝突。
2. **形容詞・形容動詞の活用語尾と熟語の衝突**：「しずか**な**」「ゆたか**な**」等の活用語尾「かな」が熟語`仮名`と、「つめた**い**」＋「**み**ず」のような単語境界をまたいだ「いみ」が熟語`意味`と、それぞれ意図せず一致し誤変換された。
3. **その他**：熟語辞書どうしの同音異義語衝突（`委員会`(いいんかい)＝`医院`+かい）や、reading項目に余分な文字が混入していたケースも発見。

**調査方法**：
1. 既知の誤変換語（未知・仮名・意味・事故）を全文検索。
2. `kanji-data.js`から変換辞書を再構築し、同じ読みを持つ熟語ペア（同音異義語衝突候補）111件を洗い出し、両方が実際に使用されているペアを重点確認（→新規に1件発見）。
3. 「熟語＋かな1〜2文字＋熟語」が挟み込まれるパターンを全文かな文の地の文に対して機械スキャン（新規バグなし、既知の1件を再検出のみ）。
4. `sentence-data-alt.js`（復習モード代替問題）についても同様に確認 → **該当なし**（今回の誤変換は`sentence-data.js`のみ）。

**発見した誤変換 20件と修正内容**：

| # | 対象漢字 | 学年 | 誤り | 修正後 |
|---|---|---|---|---|
| 1 | 暗 | 3 | `{{暗い}}よ未知` | `{{暗い}}夜道`（暗い夜道） |
| 2 | 曲 | 3 | `未知が{{曲がる}}` | `道が{{曲がる}}` |
| 3 | 転 | 3 | `未知で{{転}}ぶ` | `道で{{転}}ぶ` |
| 4 | 平 | 3 | `{{平ら}}な未知` | `{{平ら}}な道` |
| 5 | 焼 | 4 | `さ仮名を{{焼く}}` | `魚を{{焼く}}` |
| 6 | 清 | 4 | `{{清}}ら仮名みず` | `{{清}}らかな水` |
| 7 | 静 | 4 | `{{静}}仮名図書館` | `{{静}}かな図書館` |
| 8 | 冷 | 4 | `{{冷}}た意味ず` | `{{冷}}たい水` |
| 9 | 険 | 5 | `{{険しい}}やま未知` | `{{険しい}}山道` |
| 10 | 豊 | 5 | `{{豊}}仮名自然` | `{{豊}}かな自然` |
| 11 | 夢 | 5 | `{{夢}}を仮名える` | `{{夢}}をかなえる`（"叶"は未収録漢字のためかな表記のまま） |
| 12 | 迷 | 5 | `未知に{{迷う}}` | `道に{{迷う}}` |
| 13 | 危 | 6 | `{{危}}な意味ち` | `{{危}}ない道`（利用者ご指摘の例） |
| 14 | 狭 | J2 | `{{狭}}意味ち` | `{{狭}}い道` |
| 15 | 愚 | J2 | `{{愚}}仮名坑道` | `{{愚}}かな行動`（利用者確認済み） |
| 16 | 塞 | J3 | `未知を{{塞ぐ}}` | `道を{{塞ぐ}}` |
| 17 | 爽 | J3 | `{{爽}}や仮名風邪` | `{{爽}}やかな風`（"風邪"ではなく"風"） |
| 18 | 諮 | J2 | `医院かいへの{{諮問}}` | `委員会への{{諮問}}` |
| 19 | 己 | 6 | `{{自己}}唱歌いする` | `{{自己}}しょうかいする`（"紹介"は未収録漢字のためかな表記） |
| 20 | 啓 | J2 | `{{自己啓発}}` reading:`じことけいはつ` | reading（読み）のみ`じこけいはつ`に修正（tplは変更なし。余分な「と」が混入していた） |

- いずれも出題対象の漢字・読み自体は正しく、**問題文中の地の文（対象外の部分）のみ**の誤り。出題ロジック・正誤判定への影響はない。
- 全て`sentence-data.js`（予習・復習共通の元データ）に存在し、`sentence-data-alt.js`には無かった。

### 4.2.8 送りがな下線の取りこぼし修正と波線表示（v2.11・実装完了）

**背景**：利用者から「送り仮名の問題として成立していない」との指摘。§4.2.2（v2.9）の自動変換は`kanji-data.js`の**登録訓読みと完全一致する送りがな**しか`{{ }}`に取り込めず、**活用形・名詞化形を取りこぼしていた**（例：登録は「こた-**える**」だが例文は「答**え**をかく」＝名詞形のため不一致）。全学年（小2〜中3）・両モードのデータを再調査した。

なお、指摘のあった「数える」自体は既にv2.9で修正済み（`{{数}}を{{数える}}` reading `["かず","かぞえる"]`）であり、利用者環境で旧表示に見えていたのは**ブラウザ/Service Workerの古いキャッシュ**と考えられる（§4.2.5・4.2.6と同じ事象）。

#### (1) 送りがなを`{{ }}`に含める修正（41件）

**A. 単独で活用する語・名詞化形（35件）** — 漢字＋送りがな全体を`{{ }}`に含める。

| 学年 | 漢字 | 現在 | 変更後（正解） |
|---|---|---|---|
| 2 | 近 | `{{近}}くのみせ` | `{{近く}}のみせ`（近く） |
| 2 | 答 | `{{答}}えをかく` | `{{答え}}をかく`（答え） |
| 2 | 通 | `がっこうに{{通}}う` | `がっこうに{{通う}}`（通う） |
| 3 | 祭 | `なつ{{祭}}り` | `なつ{{祭り}}`（祭り） |
| 3 | 始 | `授業が{{始}}まる` | `授業が{{始まる}}`（始まる） |
| 3 | 代 | `せんせいの{{代}}わり` | `せんせいの{{代わり}}`（代わり） |
| 3 | 転 | `道で{{転}}ぶ` | `道で{{転ぶ}}`（転ぶ） |
| 3 | 流 | `かわの{{流}}れ` | `かわの{{流れ}}`（流れ） |
| 3 | 動 | `からだを{{動}}かす` | `からだを{{動かす}}`（動かす） |
| 3 | 開 | `まどを{{開}}ける` | `まどを{{開ける}}`（開ける） |
| 3 | 実 | `いねが{{実}}る` | `いねが{{実る}}`（実る） |
| 3 | 消 | `電気を{{消}}す` | `電気を{{消す}}`（消す） |
| 4 | 加 | `仲間に{{加}}わる` | `仲間に{{加わる}}`（加わる） |
| 4 | 願 | `{{願}}いごとをかく` | `{{願い}}ごとをかく`（願い） |
| 4 | 好 | `野球が{{好}}き` | `野球が{{好き}}`（好き） |
| 4 | 残 | `きゅうしょくを{{残}}す` | `きゅうしょくを{{残す}}`（残す） |
| 4 | 照 | `ライトが{{照}}らす` | `ライトが{{照らす}}`（照らす） |
| 4 | 清 | `{{清}}らかな水` | `{{清らか}}な水`（清らか） |
| 4 | 戦 | `{{戦}}いにかつ` | `{{戦い}}にかつ`（戦い） |
| 4 | 争 | `{{争}}いをとめる` | `{{争い}}をとめる`（争い） |
| 4 | 冷 | `{{冷}}たい水` | `{{冷たい}}水`（冷たい） |
| 4 | 静 | `{{静}}かな図書館` | `{{静か}}な図書館`（静か） |
| 4 | 試 | `わざを{{試}}す` | `わざを{{試す}}`（試す） |
| 4 | 治 | `かぜが{{治}}る` | `かぜが{{治る}}`（治る） |
| 4 | 訓 | `{{訓読}}みをおぼえる` | `{{訓読み}}をおぼえる`（訓読み） |
| 5 | 混 | `えのぐを{{混}}ぜる` | `えのぐを{{混ぜる}}`（混ぜる） |
| 5 | 破 | `風船が{{破}}れる` | `風船が{{破れる}}`（破れる） |
| 5 | 肥 | `{{肥}}えたつち` | `{{肥えた}}つち`（肥えた） |
| 5 | 豊 | `{{豊}}かな自然` | `{{豊か}}な自然`（豊か） |
| 5 | 易 | `{{易}}しい問題` | `{{易しい}}問題`（易しい） |
| 5 | 構 | `バットを{{構}}える` | `バットを{{構える}}`（構える） |
| 5 | 志 | `医者を{{志}}す` | `医者を{{志す}}`（志す） |
| 5 | 断 | `さそいを{{断}}る` | `さそいを{{断る}}`（断る） |
| 5 | 逆 | `{{逆立}}ちをする` | `{{逆立ち}}をする`（逆立ち） |
| 6 | 危 | `{{危}}ない道` | `{{危ない}}道`（危ない） |

**C. 中学・代替問題（6件）**

| 学年 | 漢字 | 現在 | 変更後（正解） |
|---|---|---|---|
| J1 | 嫌 | `野菜が{{嫌}}い` | `野菜が{{嫌い}}`（嫌い） |
| J2 | 狭 | `{{狭}}い道` | `{{狭い}}道`（狭い） |
| J2 | 愚 | `{{愚}}かな行動` | `{{愚か}}な行動`（愚か） |
| J3 | 爽 | `{{爽}}やかな風` | `{{爽やか}}な風`（爽やか） |
| 4 | 望 | `{{望}}み画家なう` | `{{望み}}がかなう`（望み）※**誤変換も同時修正**（「のぞみ**がか**なう」の「がか」が`画家`に誤変換されていた。§4.2.7と同一クラスの積み残し） |
| 6 | 宙(代替) | `ゆかで{{宙返}}り` | `ゆかで{{宙返り}}`（宙返り）※`sentence-data-alt.js` |

#### (2) 複合動詞・補助動詞の下線範囲（B群7件・**案1で確定**）

対象：受（うけとる）／申（もうしこむ）／追（おいかける）／急（いそいではしる）／似（にている）／久（ひさしぶり）／沿（そってあるく）

これらは「漢字の語」が後続語と連結しており、下線をどこまで引くか一意に決まらないため、3案を比較検討した。

| 案 | 内容 | 例（受） | 長所 | 短所 |
|---|---|---|---|---|
| **案1（採用）** | 漢字＋自身の送りがなまで下線。**例文は変更しない** | <u>うけ</u>とる → 受け | 例文を変えずに済む。答えは一意 | 語の途中で下線が切れ不自然。「にて」等は特に違和感 |
| 案2（不採用） | 例文を単純な形に差し替える | <u>うける</u> → 受ける | 送りがな学習として最も明確。全件が統一ルールになり例外が消える | 例文7件の書き換えが必要。「追いかける」等の実用表現は学べない |
| 案3（不採用） | 複合語全体を1つの下線にする | <u>うけとる</u> → 受けとる | 例文が自然。実用表現を学べる | 後続語を漢字で書くべきか曖昧（「受け取る」は正解か？）。紙の自己採点で判定が揺れる |

**採用理由**：既存の例文（実際に使われる自然な表現）を保てること。下線が語中で切れる見た目の不自然さは許容する。

**B群 確定内容（7件）**

| 学年 | 漢字 | 現在 | 変更後（正解） |
|---|---|---|---|
| 3 | 受 | `プレゼントを{{受}}けとる` reading `う` | `プレゼントを{{受け}}とる` reading `うけ`（受け） |
| 3 | 申 | `たいかいに{{申}}しこむ` reading `もう` | `たいかいに{{申し}}こむ` reading `もうし`（申し） |
| 3 | 追 | `いぬを{{追}}いかける` reading `お` | `いぬを{{追い}}かける` reading `おい`（追い） |
| 3 | 急 | `{{急}}いではしる` reading `いそ` | `{{急いで}}はしる` reading `いそいで`（急いで） |
| 5 | 似 | `母親に{{似}}ている` reading `に` | `母親に{{似て}}いる` reading `にて`（似て） |
| 5 | 久 | `{{久}}しぶりにあう` reading `ひさ` | `{{久しぶり}}にあう` reading `ひさしぶり`（久しぶり） |
| 6 | 沿 | `かわに{{沿}}ってあるく` reading `そ` | `かわに{{沿って}}あるく` reading `そって`（沿って） |

- 「久しぶり」は複合語だが一語として定着した名詞のため、全体を1つの下線とする。
- て形（急いで・似て・沿って）は活用語尾まで含める。後続の補助動詞・別動詞（はしる・いる・あるく）は下線外のまま。

#### (3) 送りがな問題の下線を波線にする

**目的**：「送り仮名も含めて書く問題」であることを見た目で区別し、送りがなの学習であると児童に伝わるようにする。

- **判定方法**：`{{ }}`の中身が「漢字＋ひらがな」で終わっていれば送りがな問題とみなす（例：`{{数える}}`→該当、`{{公園}}`→非該当）。**データ側に追加の属性は不要で、描画時に自動判定できる**。
- **表示**：該当する下線を**波線**（`text-decoration-style: wavy`）にする。非該当（従来どおりの熟語・送りがな無し訓読み）は実線のまま。
- 点線（`dotted`）への変更はCSS1語の修正で可能。
- 答え合わせ画面の正解表示（`.tgt`＝色付き）は現状どおり変更しない。

#### (4) 実装内容（v2.11）
- **データ修正 計48件**（A群35＋B群7＋C群5＝`sentence-data.js` 47件、`sentence-data-alt.js` 1件）。要件定義書で確定した内容を`scripts/apply_okuri_v211.py`に定義し、**変更前の行と完全一致した場合のみ置換する方式**で適用（不一致0件を確認してから反映）。
- **波線表示**：判定関数`isOkuriTarget(target)`を新設し、`fmtQuestion()`で`{{ }}`ごとにクラスを出し分け（該当時`tgt-u tgt-u-okuri`、非該当は`tgt-u`のまま）。CSSは`.tgt-u-okuri { text-decoration-style: wavy; text-decoration-thickness: 3px; text-underline-offset: 6px; }`。
  - 太さは実線（3px）と比較検証し、2pxでは細く見えたため**3pxを採用**。
  - 全1131件を走査し、波線判定279件／実線858件。熟語タイプで波線判定された2件（訓読**み**・伝**える**）はいずれも実際に送りがなを持つため正しい判定であることを確認。
- **検証**：ブラウザ実機で出題画面・答え合わせ画面（PC幅／タブレット幅768px）を確認。「数」が`かず`（実線）と`かぞえる`（波線）に自動で出し分けられること、復習モードの整合性チェック（未習漢字を含む出題0件）も合格。
- `sentence-data.js?v=9`、`sentence-data-alt.js?v=3`、`sw.js`キャッシュ v12→v13。

### 4.2.9 データ検証ハーネスの導入と誤変換2件の修正（v2.12・実装完了）

**背景**：利用者から「まめを似る」（正しくは「まめを煮る」）の誤字報告。似（に-る）と煮（に-る）が同一の訓読みを持つ、単漢字どうしの同音異義語衝突が原因（§4.2.7・4.2.8で見つけた「未知/道」「仮名/かな」等と同じバグの系統だが、単漢字どうしの衝突は初検出）。

これを機に、**過去に何度も使い捨てで書いてきた検証スクリプトを`scripts/check_data.py`として1本化**した（AIエージェント開発における「ハーネス」の実例）。

- **常時OK/NGを機械判定できる項目**（終了コードに反映）：
  1. 波括弧の対応、行フォーマットの一致
  2. `{{}}`の数とreadingの数の一致
  3. 既知の誤変換ワードの再発（回帰）チェック（未知・仮名・意味・事故く・医院かい・唱歌い）
  4. 復習モードで未習漢字を含む出題が出ないか
- **人の目視確認が必要な項目**（`[REVIEW]`として常に一覧表示。件数があること自体は異常ではない）：
  5. 熟語辞書内の同音異義語衝突候補（読みが同じ熟語が複数登録されている）
  6. **単漢字（送りがな付き訓読み）どうしの同音異義語衝突候補**（v2.12で新規追加。「似る/煮る」はここに該当）
  7. 「熟語＋かな1〜2文字＋熟語」の挟み込みパターン
- 5〜7は「正誤を確定できない」性質のため、意図的に終了コード（NG判定）には含めない設計とした。ハーネスが自動判定できるのは構文的な誤りまでで、**文脈の自然さの判断は人が行う**（§4.2.7で整理した限界を踏襲）。

**発見・修正した誤り（2件）**：

| 漢字 | 誤り | 正しい内容 |
|---|---|---|
| 豆 | `{{豆}}を似る` | `{{豆}}を煮る`（利用者ご指摘） |
| 司 | `{{司会}}を努める` | `{{司会}}をつとめる`（ハーネスの新規チェックで発見。「務める」が正しいが、司＝小4に対し務＝小5配当のため漢字で書けず、かな表記に修正） |

- ハーネス自体にも開発中に2つの不具合があった：①「画家」を既知の誤変換ワードとして単純な文字列一致でチェックしていたため、正当な用例（有名な画家）まで誤検知した→ ambiguous な語はリストから除外し、文脈依存の判定（項目5・6）に委ねる方針に修正。②行番号をコメント除去後のテキストから計算しており、実際のファイルの行番号とズレていた→元ファイルから計算するよう修正。
- v2.12適用後、9組（熟語）＋15組（単漢字）の同音異義語候補を全件目視確認し、いずれも文脈上正しい使用であることを確認済み。
- `sentence-data.js`の内容変更のみ（`{{}}`・reading構造は変更なし）。

### 4.3 出題ロジック（適応的出題）
1. **過去に間違えた問題を、§4.3.2の間隔反復スケジュールに基づき優先**して当日の出題に含める。ただし§4.3.1の上限を超えない範囲とする。
2. 残りは**正解率の低い漢字・未出題の漢字を優先**して選出。
3. 問題タイプ（書き・熟語・用例）をバランスよく混ぜる。
4. 出題順はシャッフルする。

### 4.3.1 過去に間違えた問題の出題比率上限（v2.10 仕様変更・実装完了）

**目的**：復習（間違え直し）の比重が大きくなりすぎて、新しい漢字を学ぶ機会が圧迫されないようにする。

- 1回の出題に占める「過去に間違えた問題（§4.3.2の間隔反復スケジュール対象）」の割合は、出題総数の**30%まで**とする（小数点以下切り捨て）。
  - 例：10問出題 → 過去に間違えた問題は**最大3問**まで
  - 例：20問出題 → 過去に間違えた問題は**最大6問**まで
- 上限を超えて「本日が出題対象日」の間違い問題がある場合は、§4.3.2のスケジュールで**対象日を過ぎている日数が長いもの**を優先し、残りは翌日以降に持ち越す（消えずに繰越し）。
- 予習モード・復習モードの両方に適用する（現状どちらも「前日不正解」を優先する仕組みを持っているため、両方を§4.3.2の間隔反復ベースに置き換える）。

### 4.3.2 間違えた問題の間隔反復（スペースド・リピティション）出題（v2.10 仕様変更・実装完了）

**目的**：一度間違えた漢字を、間隔をあけて繰り返し出題する（間隔反復学習法）ことで、短期的な暗記でなく長期記憶への定着を図る。

- **頻度ステージ**：以下8段階を、間隔の短い順（ステージ0〜7）とする。
  ステージ0＝翌日／1＝3日後／2＝5日後／3＝1週間後／4＝10日後／5＝15日後／6＝20日後／7＝30日後
- **開始**：まだ追跡対象でない漢字（`key`単位、既存`stats`と同じ粒度）を初めて間違えると、**ステージ0（翌日）**から追跡を開始する。次回の出題対象日＝間違えた日＋ステージ0の日数。
  - すでに追跡中の漢字を間違えた場合は、この「開始」ではなく下記「対象日に間違えた場合」（1つ前のステージに戻す）を適用する。
- **対象日に正解した場合**：連続正解数を+1し、
  - **連続正解2回で打ち切り**（追跡対象から外し、通常の正解率ベース出題§4.3の2に戻る）。
  - 2回に満たなければ、ステージを1つ進め（上限はステージ7）、次回対象日＝その正解した日＋新ステージの日数。
- **対象日に間違えた場合**：連続正解数を0にリセットし、**ステージを1つ戻す**（下限はステージ0）。次回対象日＝その間違えた日＋新ステージの日数。
  - 例：ステージ3（1週間後）で間違えた → ステージ2（5日後）に戻り、その日から5日後が次の対象日。
  - ステージ0で間違えた場合はステージ0のまま（翌日が次の対象日）。
- **対象日の扱い**：対象日当日に出題できなかった場合（アプリを開かなかった等）は、次に開いた日に「出題待ち」として繰り越す（消滅させない）。
- **対象範囲**：予習モード・復習モードの両方に適用する（両モードとも同じ間隔反復の追跡データを参照・更新する）。
- **データ保持**：漢字（`key`）ごとに「現在のステージ（0〜7）」「次回対象日」「連続正解数（0〜1）」を保持する。既存の`stats`（tested/correct集計）とは別に、間隔反復専用の状態を新設する。
- **§4.3.1との関係**：ある日に対象日を迎える間違い問題が§4.3.1の上限（30%）を超える場合は、対象日超過日数が長い方を優先し、残りは翌日以降に繰り越す。

### 4.3.3 §4.3.1・4.3.2の実装内容（v2.10）
- プロフィールごとのデータに`srs`（漢字keyごとに`{stage, correctStreak, nextDue}`）を新設。既存の`history`（前日不正解の判定に使用）に代わり、これを唯一の情報源として出題判定する（旧`Store.getYesterdayWrong()`は削除）。
- 正誤の記録時（`Store.recordAnswer`と同じタイミング）に`Store.recordSrsResult(key, correct)`を呼び、ステージ・次回対象日を更新する。
- 出題選定（`Quiz.selectSrsDue(cap)`）は、対象日を迎えた漢字を超過日数の降順で取得し、上限件数までを返す。未習の相方を含む場合は既存の代替問題（`getReviewCandidate`）で救済し、救済できないものは見送り（対象日はそのままのため自然に繰り越される）。
- **実装中に発見・修正した不具合**：日付計算で`Date.toISOString()`を使うとUTCに変換されるため、JST（UTC+9）環境では計算結果が1日ずれるバグがあった。ローカルの年月日を直接組み立てる方式に修正し、ブラウザ実機で検証済み。
  - 同種のバグが既存の`weekKey()`（週の習慣バッジ集計）にも見つかったが、本v2.10の対象外のため別途対応する。

## 5. 採点・記録仕様（v2.0 中核）

### 5.1 答え合わせフロー（v2.1 改訂：一括答え合わせ方式）
```
1. PC画面に第1問〜第10問を連続で出題（答えは見せない）
2. 児童は紙に①〜⑩の番号をつけて漢字を書いていく
3. 10問終了後、「答えあわせ」画面で全10問の正解を一覧表示
4. 児童本人 or 保護者が紙と正解を照合
5. 各問題の ⭕（正解）／❌（不正解）を選んで記録
6. 全問チェック後「きろくする」で保存 → 結果画面へ
```
- 出題中は「まえへ」ボタンで前の問題に戻れる（テスト中の見直し用）。
- ⭕❌を10問すべて選ぶまで記録ボタンは押せない（未採点防止）。

### 5.2 記録項目
| 項目 | 内容 |
|------|------|
| 日付 | 実施日（YYYY-MM-DD） |
| 漢字＋学年 | 出題された漢字と配当学年 |
| 問題タイプ | 書き／熟語／用例 |
| 正誤 | ○（正解）／×（不正解）※本人・保護者が入力 |

- 記録は端末内（localStorage）に保存し、外部送信しない。
- **（v2.4）記録は実施時にアクティブだったプロフィール（1号／2号／3号）に紐づけて保存する**（§3.5.3）。

### 5.2.1 「今日の日付」判定のタイムゾーンバグ修正（v2.10.2・実装完了）

**症状**：利用者からの報告により発覚。日本時間（JST）の深夜0時〜朝8時59分の間にアプリを開くと、前日に学習済みであるにもかかわらず「本日は学習済み」という表示になり、当日分の出題ができなかった。

**原因**：「今日の日付」を`Date.toISOString().slice(0,10)`で計算していたため、UTCに変換されてしまい、JST（UTC+9）環境では深夜0時〜朝8時59分の間、実際の日付より1日前の日付が「今日」として扱われていた（UTC上ではまだ前日のため）。
- 影響箇所：`Store.today()`（全ての日付判定の起点）、連続日数(ストリーク)判定、週間せいせき集計（§6.2）、`weekKey()`（週の習慣バッジ集計、§6.2.1）、間隔反復の対象日計算（§4.3.2、v2.10で新設した`addDaysToDateStr()`は先に同種の修正済み）。

**修正内容**：ローカルの年月日を直接組み立てる共通関数`formatLocalDate(Date)`を新設し、上記すべての日付計算をこれに統一。ブラウザ実機で、JST朝7時を模した日時での再現テスト（修正前後の比較）、および実際の症状（前日学習済み→当日「学習済み」の誤表示）が解消することを確認済み。

### 5.3 テストの見返し（v2.5 新規・実装完了）

**背景・課題**：現状は結果画面（§5.1の手順6）を離れると、その回にどんな問題が出て、どう答えたかを二度と確認できない。「答え合わせをした後、問題を見返したい」という要望に対応する。

**仕様（確定・v2.5改訂：カレンダー経由の見返しを追加）**
- **アクティブなプロフィールの、いつのテストでも見返せるようにする。**（直近2回はホーム画面から即アクセス、それ以前も含む全履歴はカレンダー経由でアクセス）
- **エントリーポイント**
  1. **結果画面**：現在の「まちがえた問題」一覧に加え、**「📖 この回をぜんぶ見返す」ボタン**を新設し、その場で見返し画面（下記）に遷移できるようにする。
  2. **ホーム画面**：新しいカード「📖 テストの見返し」を追加し、アクティブなプロフィールの**直近2回分**（実施日・スコア）をボタン形式で並べる。タップすると見返し画面に遷移する。
     - 例：`[7/4(金) 7/10問正解]` `[7/3(木) 9/10問正解]`
     - 実施回数が2回未満の場合は、ある分だけ表示する（0回なら本カード自体を表示しない）。
  3. **学習カレンダー（v2.5拡張）**：✅の付いた日をタップした際に表示される「その日のスコア」に加えて、**「📖 この日の問題を見返す」ボタン**を追加する。これにより**カレンダーに記録が残っている全期間**のテストを見返せる（直近2回に限らない）。
- **見返し画面（新規）**
  - ヘッダーに対象日・スコア（例：「2026-07-04のテスト　7/10問正解」）を表示。
  - その回の**全問題を出題順に一覧表示**し、各問題に以下を表示する：
    - 問題文（対象箇所を漢字にした完成形の文）
    - 自己採点の結果（⭕ せいかい／❌ まちがい のバッジ）
  - 「🏠 ホームにもどる」ボタンで戻る（カレンダー経由で開いた場合はカレンダーへ戻る）。
- **データ**：既存の学習履歴（`history[日付] = {questions, answers, score}`、§3.5.3）をそのまま利用する。**データ構造の変更は不要**（後方互換）。カレンダーには元々全期間の履歴が保持されているため、見返し画面を日付指定で呼べる形にするだけで全履歴に対応できる。

---

## 6. 可視化仕様

### 6.1 ヒートマップ（進捗マップ）
- **（v2.4）アクティブなプロフィール（1号／2号／3号）ごとに独立して表示する。**
- 学年タブ（2〜5年生）で切り替え。
- 各漢字を正解率で色分け：
  | 色 | 意味 |
  |----|------|
  | グレー | 未出題 |
  | 赤 | 正解率 低（〜50%未満） |
  | 黄 | 正解率 中（50〜80%） |
  | 緑 | 正解率 高（80〜100%未満） |
  | 濃緑 | 完璧（100%） |
- セルタップで、その漢字の読み・熟語・成績を表示。

### 6.2 進捗・達成感の演出
| 要素 | 内容 |
|------|------|
| 連続日数（ストリーク） | 毎日の継続を🔥で表示 |
| バッジ | §6.2.1のバッジ体系で獲得（**v2.6で大幅拡張**） |
| 週間成績 | 直近7日の解答数・正解数・正解率 |
| 完了バナー | 当日のテスト完了を明示し達成感を付与 |

### 6.2.1 バッジ体系（v2.6 新規・実装完了）

**背景・課題**：現行バッジは「連続日数」中心（3/7/30/100日連続）＋累計解答数（50/200問）の6種のみ。毎日は難しくても週に数回続けられている子や、連続は途切れても長く学習を続けている子を称える仕組みがなかった。**「毎日でなくてもコツコツ続けている」ことを多面的に称える**よう、5カテゴリへ拡張する。

**設計方針**
- **連続（ストリーク）だけに偏らない。** 「連続」「累計回数」「週の習慣」「長期継続」「複数学年」の**5つの観点**で継続を評価する。
- **最初はこまめに、だんだん間隔を広げる。** 学習を始めたばかりの子がすぐ達成感を得られるよう、序盤のバッジは細かく（5回ごと等）配置し、習慣化を後押しする。
- **獲得済みも未獲得も一覧表示（ラベル付きコレクション）。** 未獲得はグレー表示にし「次はこれを目指す」と分かるようにして、継続の動機付けにする。

**バッジ一覧（アイコンは実装時に微調整）**

**A. 連続日数（ストリーク）** — 既存を継承
| バッジ | 条件 |
|---|---|
| 🔥 れんぞく3日 | 3日連続 |
| ⚡ れんぞく7日 | 7日連続 |
| 🥇 れんぞく30日 | 30日連続 |
| 👑 れんぞく100日 | 100日連続 |

**B. がんばった回数（累計実施回数）** — NEW（「10回目実施」など連続以外の継続を称える。序盤こまめに）
| バッジ | 条件（テスト実施した日数の累計） |
|---|---|
| 🌱 はじめの一歩 | 1回 |
| 🌿 5回 | 5回 |
| 🍀 10回 | 10回 |
| 🌸 15回 | 15回 |
| 🌻 20回 | 20回 |
| ⭐ 30回 | 30回 |
| 🌟 50回 | 50回 |
| 💯 100回 | 100回 |
| 🏆 200回 | 200回 |
| 🎖️ 365回 | 365回 |

**C. 週の習慣（週3回以上）** — NEW（毎日でなくても週3回続けられた「週」を積み重ねで称える）
| バッジ | 条件（週3回以上できた週の累計数）|
|---|---|
| 📅 はじめての習慣週 | 週3回以上を1週 達成 |
| 📗 習慣4週 | 4週 達成 |
| 📘 習慣12週 | 12週 達成 |
| 📙 習慣26週 | 26週 達成 |
| 📚 習慣52週 | 52週 達成 |
- 「週」は月曜〜日曜で区切る。その週にテストを実施した日が3日以上あれば「達成した週」とカウントする。

**D. 長く続けた証（長期継続）** — NEW（「低学年→高学年まで続けている」長い継続を表現）
| バッジ | 条件（初回実施日からの経過期間 ＋ 一定の実施回数）|
|---|---|
| 🗓️ 1ヶ月つづけた | 初回から30日以上 かつ 累計10回以上 |
| 🌗 3ヶ月つづけた | 90日以上 かつ 累計30回以上 |
| 🌙 半年つづけた | 180日以上 かつ 累計60回以上 |
| 🎊 1年つづけた | 365日以上 かつ 累計120回以上 |
- 「経過期間だけ」では長期放置でも達成できてしまうため、**期間と実施回数の両方**を条件にして「実際に続けている」ことを担保する。

**E. 複数学年チャレンジ** — NEW（低学年から高学年まで幅広く学んだ証）
| バッジ | 条件 |
|---|---|
| 🧩 2学年チャレンジ | これまでの出題で2つ以上の学年の漢字を経験 |
| 🎨 5学年チャレンジ | 5つ以上の学年を経験 |
| 🏅 全学年チャレンジ | 2年〜中学3年の全8段階を経験 |
- 各テストの「主に出題された学年」（多数を占める学年）を履歴から集計し、経験した学年の種類数で判定する。
- **（v2.7改訂）**対象学年が2〜5年（4段階）から2年〜中学3年（8段階）に増えるため、中間バッジは「5学年チャレンジ」、「全学年チャレンジ」の条件は4段階→8段階に更新する。

**データについて**
- 上記すべて**既存の学習履歴（`history[日付]={questions,answers,score}`）と統計から計算可能**。`questions[].grade` から学年、日付キーから回数・週・経過期間を算出する。**データ構造の変更は不要**（後方互換）。
- 判定・表示は**アクティブなプロフィールごと**に行う（§3.5）。

**表示（ラベル付きコレクション）**
- ホーム画面のバッジ欄を、カテゴリ別のバッジグリッドに変更する。
- 各バッジは「アイコン＋短い名前」で表示。**獲得済みは色付き、未獲得はグレー表示＋条件を小さく表示**して次の目標が分かるようにする。
- バッジ数が増えるため、ホーム内で縦にスクロールして見られる形にする（別画面化はしない）。

### 6.3 学習カレンダー（v2.2 新規）
- **（v2.4）アクティブなプロフィール（1号／2号／3号）ごとに独立して表示する。**
- 月表示のカレンダーを表示する。
- **学習を実施した日には自動でチェックマーク（✅）を付ける。**（記録保存＝実施日として即反映）
- 月の切り替え（前月・翌月）ができる。
- チェック済みの日をタップすると、その日のスコア（例：8/10）を確認できる。
- **（v2.5）スコア表示に加えて「📖 この日の問題を見返す」ボタンを表示し、タップするとその日のテスト見返し画面（§5.3）に遷移する。**
- 目的：継続の可視化による達成感の強化（ヒートマップが「どの漢字が弱いか」、カレンダーは「いつ頑張ったか」を可視化する）。

---

## 7. 漢字データ仕様

- 形式：`[漢字, 学年, [音読み], [訓読み], [[熟語, よみ], ...]]`
- 収録：小学2〜5年生の配当漢字（約550字）。
- 用途：出題文の生成、答え合わせ、一覧・モーダル表示。

### 7.1 対象学年の拡張（v2.7 新規・実装完了）

- **小学6年生**：文部科学省の学年別漢字配当表に基づく公式の6年生配当漢字（191字）をそのまま収録する。
- **中学1〜3年生**：中学校の漢字は小学校のような**国の公式な学年別リストが存在しない**（常用漢字のうち教育漢字を除いた約1,110字を3学年で学習する、という大枠のみが定められている）。そのため本アプリでは、**一般的な中学校国語教科書・漢字ドリルで各学年の「新出漢字」として共通して扱われる代表的なものを参考に、中1・中2・中3それぞれ100〜150字程度（合計300〜450字程度）を収録**する。
  - 注記：これは教育委員会・教科書会社の公式な年度配当ではなく、**アプリ独自の目安の区分**である旨を漢字いちらん画面・要件定義書に明記する。
  - **今回はこの300〜450字程度を対象範囲とし、常用漢字の残り（約1,110字）を全て網羅することはしない。** 全常用漢字への拡張は「発展編」として今後の展開（§10.2）に位置づける。
- 学年コードは文字列の配列 `['2','3','4','5','6','J1','J2','J3']` の順で管理し、「一つ下の学年」は算術（学年番号−1）ではなく**この配列上の1つ前の要素**として求める（中学1〜3年という非数値の連なりに対応するため）。
- 出題文データ（`sentence-data.js`）も同形式（`{tpl, reading, type}`）で6年生・中学1〜3年生ぶんを追加する。既存の変換スクリプト（`scripts/convert_sentences.py`）を再利用し、既習漢字への変換と同音異義語の突合検証を行う。
- **画面への影響**：学年タブ（ホーム／しんちょくマップ／漢字いちらん）に「6年」「中1」「中2」「中3」を追加する。

---

## 8. 画面構成

| 画面 | 機能 |
|------|------|
| ホーム | プロフィール切り替え（1号/2号/3号）＋学年選択（6年・中1・中2・中3を含む）＋出題数選択（10問/20問）・出題順序選択（ランダム/五十音順）・**（v2.8）学習モード選択（予習/復習）・「習った漢字を選ぶ」ボタン**を最上部に追加。以下はアクティブなプロフィールのもの：ストリーク・週間成績・バッジコレクション（ラベル付き・獲得/未獲得を一覧）・「今日の◯問」開始・直近2回のテスト見返しカード |
| **習った漢字を選ぶ（v2.8 新規）** | 現在の学年の漢字だけを一覧表示し、学校で習った漢字にチェックを入れる画面。選択件数を表示 |
| テスト | 出題数ぶんを一覧表示 → 答えあわせ画面で○×自己採点（アクティブなプロフィールに記録）。**（v2.8）復習モードでは習った漢字リストから出題** |
| 結果 | スコア・不正解一覧の振り返り・「この回を見返す」ボタン |
| 学習カレンダー | 月表示・実施日への自動チェック・その日のスコア確認・「この日の問題を見返す」ボタン（全履歴に対応） |
| テスト見返し | 指定した回の全問題・完成文・⭕❌の結果を一覧表示。ホーム／結果画面／カレンダーいずれからも同じ画面を呼び出す |
| 進捗マップ | アクティブなプロフィールの学年別ヒートマップ |
| 漢字一覧 | 学年別の漢字グリッド・詳細モーダル（**プロフィール共通の参照データ**） |

---

## 8.5 タブレット/スマホ対応（v2.3　実装・公開完了）

> **公開URL：https://koishi3dayo.github.io/kanji-drill/**
> スマホ/タブレットのブラウザで開き「ホーム画面に追加」でアプリとして利用可能。

### 8.5.1 方針（確定）
- **配信方法：無料の静的ホスティング（GitHub Pages）＋ PWA化。**
  - URLでどこからでもアクセス可能。PCがオフでも使える。
  - ホーム画面にアイコンを追加し、ブラウザのURLバーなしで全画面起動。オフラインでも起動可能。
- **データ共有：行わない（1台運用）。** 学習記録はその端末内（localStorage）のみ。端末をまたいだ同期はしない。
  - → 「学習データは端末内のみ・外部送信なし」のプライバシー方針を維持できる。

### 8.5.2 変更内容

**A. レスポンシブ／タッチ対応（CSS中心）**
- タップ領域を最低44px確保（⭕❌ボタン・カレンダー日付・ヒートマップセル・タブ）。
- iOS Safari対策：`viewport-fit=cover` ＋ `safe-area-inset`（ノッチ/ホームバー回避）、`100dvh` 採用、ダブルタップ拡大の抑制（アクセシビリティのピンチズームは残す）。
- 狭い画面（〜360px）向けのフォント・余白の調整、一覧グリッドの列数自動化。

**B. PWA化**
| ファイル | 役割 |
|---------|------|
| `manifest.json` | アプリ名・アイコン・全画面表示（standalone）・テーマ色・縦向き固定 |
| アイコン（192/512px, maskable） | ホーム画面アイコン。iOS用 `apple-touch-icon` も設定 |
| `sw.js`（Service Worker） | `index.html`・データ・アイコンをキャッシュし**オフライン起動**を実現 |
- 更新時の対策：Service Workerのキャッシュにバージョンを持たせ、新版公開時に自動更新する。

**C. 配信（GitHub Pages）**
- `kanji-drill` をGitリポジトリ化し、GitHubへpush → Pagesを有効化。
- 公開URL例：`https://koishi3dayo.github.io/kanji-drill/`
- **注意**：公開されるのはアプリのコード（漢字ドリル本体）のみ。**個人情報・学習記録は含まれない**（記録は各端末のlocalStorageに留まるため）。ただしURLを知れば誰でもアプリを開ける状態にはなる。

### 8.5.3 動作確認方針
- プレビューを 375px（スマホ）・768px（タブレット）で検証。
- タッチ操作（⭕❌・タブ・カレンダー）・全画面表示・オフライン起動を確認。

### 8.5.4 実装状況（2026-07-04 時点）
- **A（レスポンシブ／タッチ）実装・検証済み**：375px/768pxで表示確認。safe-area・44pxタップ領域・狭幅調整を適用。
- **B（PWA化）実装・検証済み**：`manifest.json`・`sw.js`・アイコン（192/512/512-maskable/180）を追加。SW登録・全アセットのキャッシュ（オフライン起動可能）を確認。生成スクリプトは `scripts/make_icons.py`。
- **C（GitHub Pages公開）完了（2026-07-04）**：リポジトリ `github.com/koishi3dayo/kanji-drill`（公開）を作成しpush、GitHub Pagesを有効化。公開URL `https://koishi3dayo.github.io/kanji-drill/` で全アセット（HTML・データ・manifest・SW・アイコン）の配信を確認済み。個人情報・学習記録はリポジトリに含まない（記録は各端末のlocalStorageのみ）。

---

## 9. 技術・非機能要件

| 項目 | 内容 |
|------|------|
| 現行実装 | HTML/CSS/JavaScript（単一ページ、サーバー不要） |
| データ保存 | ブラウザ localStorage（オフライン動作） |
| 対応端末 | PC（主）。**タブレット/スマホ対応を追加予定（v2.3 設計・承認待ち、§8.5）** |
| 起動方法 | ローカルHTTPサーバー（python -m http.server）。**v2.3でPWA＋GitHub Pages配信を追加予定** |
| プライバシー | 学習データは端末内のみ。外部送信なし（v2.3の1台運用でも維持） |

---

## 10. 今後の展開（ロードマップ）

| フェーズ | 内容 |
|---------|------|
| Phase 1（完了） | 出題・自己採点・ヒートマップの基本機能 |
| Phase 2 | ○×自己採点UIへの改修（本仕様 v2.0 反映） |
| Phase 3 | 問題文の自然化、選択式モードの追加検討 |
| Phase 4 | UX磨き込み（音声読み上げ・アニメーション） |
| Phase 5 | モバイルアプリ化（React Native） |
| Phase 6（発展編・将来検討） | 中学の収録範囲を、常用漢字の残り約1,110字までフル網羅する形に拡張する（§7.1・§10.1で今回は300〜450字程度の目安収録にとどめている） |

### 10.1 v2.7 実装まとめ（実装完了）

**変更点の一覧**
1. `kanji-data.js` / `sentence-data.js` に **小学6年生（191字・公式配当）＋中学1〜3年生（各100〜150字・アプリ独自の目安収録、合計300〜450字、§7.1）** を追加。
2. 学年を数値ではなく `['2','3','4','5','6','J1','J2','J3']` の**配列順**で管理する共通ヘルパーを導入し、「一つ下の学年」を配列上の前要素として求めるよう `Quiz.slotPlan` を改修（中学2年→中学1年、中学3年→中学2年、中学1年→6年、6年→5年に対応）。
3. プロフィール設定に **`qCount`（10/20）** と **`orderMode`（random/gojuon）** を追加し、ホーム画面に選択UIを設置。
4. `Quiz.selectDaily` を `qCount` に応じた比例配分（10問=9:1、20問=18:2 等）に対応させ、`orderMode` が `gojuon` の場合は最終シャッフルの代わりに **学年配当順（データ収録順）** で並べ替える。
5. 「10問」を前提にしていたUI文言（ホームの開始ボタン・出題中の紙のヒント・結果画面の分母表示等）を**すべて実際の問題数を参照する動的表示**に変更。
6. バッジ「複数学年チャレンジ」の階層条件を、対象学年数の増加（4→8段階）に合わせて更新。

**影響が大きい作業（工数の見積り）**
- 新規漢字データ（6年生191字＋中学1〜3年合計300〜450字、総計約500〜650字）の読み・熟語・出題短文の作成が最大の工数。6年生は公式配当のため対象漢字の選定に迷いはないが、中学1〜3年は§7.1の注記どおり「アプリ独自の目安」である点を踏まえて収録する。
- 既存の安全な変換スクリプト（`scripts/convert_sentences.py`：助詞誤変換を避けるため2文字以上の熟語のみを既習漢字化）をそのまま再利用できるため、文章の質は既存学年と同水準を維持できる見込み。
- **常用漢字の完全網羅（約1,110字）は今回のスコープ外**。将来の発展編（Phase 6）として別途対応する。

**データ構造への影響**
- `qCount`・`orderMode` はプロフィールへの**追加フィールドのみ**（後方互換、未設定時はデフォルト値にフォールバック）。
- 学年コードの配列管理化は**内部ロジックの一般化**であり、既存の2〜5年データ・保存済み学習履歴に影響しない。

---

## 11. 変更履歴

| 日付 | 版 | 変更内容 |
|------|----|---------|
| 2026-07-03 | v1.0 | 初版（IME入力・自動採点方式） |
| 2026-07-03 | v2.0 | **入力方式を「紙に手書き回答＋PCで○×自己採点」に変更。** 電子端末への手書き入力を廃止 |
| 2026-07-03 | v2.1 | **①10問連続出題→終了後に一括答え合わせ方式に変更**（1問ごとの答え表示を廃止）。**②出題を5〜10文字のかな短文に変更**（例：「うたをうたう」→歌を歌う）。短文データ `sentence-data.js` を全漢字ぶん新設 |
| 2026-07-03 | v2.2 | **①10問を最初から一覧表示する方式に変更**（逐次出題を廃止）。**②出題学年に重み付けを追加：5年生の漢字を10問中9問出題**。**③問題文中、出題対象以外のかなも極力既習漢字に変換して表示**（`sentence-data.js` を全550件超テンプレート形式に刷新）。**④学習カレンダーを新設し、実施日に自動チェックを付与**。全項目、実装・動作確認まで完了 |
| 2026-07-04 | v2.4 | **①3人の子ども（1号/2号/3号）ごとのプロフィール機能を新設**（ホーム画面で切り替え、進捗・カレンダー・ストリークを個別管理）。**②プロフィールごとに学年（2〜5年）を設定でき、出題の重み付けが学年に応じて変わる方式に一般化**（2年生→10:0、3〜5年生→9:1・他学年枠は主に一つ下の学年）。データ保存構造を `activeProfile` ＋ `profiles{1,2,3}` に変更（§3.5）。旧単一データは1号へ自動移行。**実装・動作確認まで完了**（全学年の出題比率・プロフィール間のデータ独立・旧データ移行を検証済み） |
| 2026-07-04 | v2.5 | **テストの見返し機能を新設**（§5.3）：結果画面に「この回を見返す」ボタン、ホーム画面に直近2回分のテスト見返しカードを追加。**さらに学習カレンダーからも「この日の問題を見返す」で全履歴を見返せるように拡張**。過去に出題された全問題・完成文・⭕❌の結果を一覧表示できるようにする。データ構造の変更なし（既存の学習履歴を流用）。**実装・動作確認まで完了**（結果画面・ホーム・カレンダーの3経路すべてで見返し画面への遷移と復帰を検証済み） |
| 2026-07-04 | v2.6 | **バッジ体系を6種→5カテゴリ26種に拡張**（§6.2.1）。連続日数に加え、①累計実施回数（序盤こまめに：1/5/10/15/20/30/50/100/200/365回）、②週3回以上の習慣週の累計、③長期継続（経過月数＋実施回数）、④複数学年チャレンジ、を新設。ホームのバッジ表示を獲得/未獲得が分かるラベル付きコレクションに変更。データ構造の変更なし（既存履歴から算出）。**実装・動作確認まで完了**（各カテゴリの判定・獲得数表示・未獲得のグレー表示を検証済み） |
| 2026-07-04 | v2.7 | **①対象学年を小学6年生・中学1〜3年生まで拡張**（§7.1。6年生は公式配当191字、中学1〜3年はアプリ独自の目安で各110字前後＝合計333字を収録。常用漢字の完全網羅は発展編として今後の展開へ）。**②出題数をプロフィールごとに10問／20問から選択可能に**（§4.1.2）。**③出題順序をプロフィールごとにランダム／五十音順から選択可能に**（§4.1.3）。学年の「一つ下」判定を配列順管理に一般化し、6年→5年・中1→6年・中2→中1・中3→中2に対応。バッジ「複数学年チャレンジ」の条件も8段階に更新。**実装・動作確認まで完了**（全学年の出題比率〈中3=9:1、中1の20問=18:2 等〉・五十音順並び・学年タブ8段階・中学の注記表示・20問での結果/見返しを検証済み）。総収録漢字1131字。実装まとめは§10.1 |
| 2026-07-05 | v2.8 | **①「習った漢字リスト」機能を新設**（§4.1.5）：現在の学年より下は自動で全て「習った」扱い、現在の学年ぶんはホーム画面の新画面「習った漢字を選ぶ」で明示的にチェック（初期状態は全て未選択）。**②予習モード／復習モードを新設**（§4.1.4）：予習モードは既存の学年重み付けロジックのまま変更なし、復習モードは習った漢字リストのみから出題し「習った漢字」かつ「前回間違えた」漢字を最優先、残りを正解率の低い漢字・未出題の漢字で補充。プロフィールに `studyMode`・`learnedByGrade` を追加（§3.5.3、後方互換）。**実装・動作確認まで完了**（復習プールの限定・「習った∩前回まちがえた」最優先・習っていない漢字の完全除外・予習モード不変・空プールガードを検証。復習の未出題スコアに乱数を追加し学年横断で満遍なく出るよう修正）。SW/キャッシュをv6/v4に更新 |
